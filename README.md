# optical-character-recognition
Yser draws a digit on a 20*20 matrix and trains the model, which the user can later test by asking the model to predict the number drawn.
The result is displayed on a probability format where the model predicts which digit is closely matched with the input image
